Store proc

ALTER PROCEDURE [dbo].AppUserAddresses_Create}

-- add parameters for the stored procedure here
-- declare the output with the parameter, so it'll get returned no matter what.
-- @Uid = NewID()

AS

BEGIN

SET @Uid = NewID()

INSERT INTO [dbo].[AppUserAddresses]
(,)


VALUES

(@, @)

